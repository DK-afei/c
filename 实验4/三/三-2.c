#include <stdio.h>
 void main()
 {
	 int x,i;
	    printf("请输入20个整数")；
    	scanf("%d",&x);
		for(;;)	
			if (a>0)

				if(a<0)

					if(a=0)

	printf("sum=%f,mul=%f,x=%f,y=%f\n",sum,mul,x,y);
 }
